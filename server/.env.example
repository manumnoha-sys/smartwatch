# Generate with: openssl rand -hex 20
API_KEY=replace_with_generated_key

# PostgreSQL password
POSTGRES_PASSWORD=replace_with_strong_password

# Set automatically by docker-compose — override only if running server standalone
# DATABASE_URL=postgresql+asyncpg://health:password@localhost:5432/healthdb

ENVIRONMENT=production
LOG_LEVEL=INFO
